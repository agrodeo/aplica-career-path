import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { ArrowLeft, Bookmark, Check, MapPin } from "lucide-react";
import { MatchInfo, SiteHeader } from "@/components/aplica";
import { Button } from "@/components/ui/button";
import { jobs } from "@/lib/aplica-data";

export const Route = createFileRoute("/jobs/$id")({ loader: ({ params }) => { const job = jobs.find((item) => item.id === params.id); if (!job) throw notFound(); return job; }, head: ({ loaderData }) => ({ meta: [{ title: loaderData ? `${loaderData.role} en ${loaderData.company} — aplica` : "Trabajo no disponible — aplica" }, { name: "description", content: loaderData?.description ?? "Esta oportunidad ya no está disponible." }, { property: "og:title", content: loaderData ? `${loaderData.role} en ${loaderData.company}` : "Trabajo no disponible" }, { property: "og:description", content: loaderData?.description ?? "Esta oportunidad ya no está disponible." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: JobDetail, notFoundComponent: () => <div className="p-12 text-center"><h1 className="text-2xl font-medium">Este trabajo ya no está disponible.</h1><Button asChild className="mt-6"><Link to="/jobs">Volver a trabajos</Link></Button></div> });
function JobDetail() { const job = Route.useLoaderData(); return <div className="min-h-screen"><SiteHeader /><main className="mx-auto max-w-[900px] px-5 py-10 md:py-16"><Link to="/jobs" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="h-4 w-4"/>Volver a trabajos</Link><div className="mt-10 flex flex-col justify-between gap-7 border-b border-border pb-9 md:flex-row"><div className="flex gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-lg border border-border bg-surface text-sm font-semibold">{job.initials}</div><div><h1 className="text-3xl font-medium tracking-normal">{job.role}</h1><p className="mt-1 text-lg">{job.company}</p><p className="mt-2 flex items-center gap-1 text-sm text-muted-foreground"><MapPin className="h-4 w-4"/>{job.location} · {job.mode} · {job.salary}</p></div></div><div className="text-left md:text-right"><p className="text-3xl font-semibold text-primary">{job.match}% match</p><MatchInfo /></div></div><section className="grid gap-10 py-10 md:grid-cols-[1fr_260px]"><div><h2 className="text-lg font-medium">Sobre el puesto</h2><p className="mt-4 leading-7 text-muted-foreground">{job.description}</p><h2 className="mt-9 text-lg font-medium">Requisitos</h2><ul className="mt-4 space-y-3">{job.requirements.map((item) => <li key={item} className="flex gap-3 text-sm"><Check className="h-4 w-4 text-success"/>{item}</li>)}</ul></div><aside><div className="rounded-xl border border-border p-5"><h2 className="font-medium">Por qué encaja con vos</h2><ul className="mt-4 space-y-3">{job.fit.map((item) => <li key={item} className="flex gap-2 text-sm text-muted-foreground"><Check className="h-4 w-4 text-success"/>{item}</li>)}</ul>{job.concern && <p className="mt-5 border-t border-border pt-4 text-xs leading-5 text-caution">{job.concern}</p>}</div><Button asChild className="mt-4 w-full"><Link to="/upgrade">Aplicar a este trabajo</Link></Button><Button variant="outline" className="mt-2 w-full"><Bookmark/>Guardar</Button></aside></section></main></div>; }
